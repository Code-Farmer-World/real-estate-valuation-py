# 版本紀錄

## v1.0.0（2026-09-13）交件版

2026 新北市 AI 智慧城市黑客松地政局命題的交件版本。
地端功能完整可交付，部署與生成式 AI 未做（見「這一版沒做的」）。

### 做了什麼

同一個規則引擎，兩個方向。

**產出模式**吃填好的表3 地價區段勘查表，算出表5-1 影響地價區域因素分析明細表
與表4 比較法調查估價表該填的每一格，產出可交件的 xlsx 與 PDF。
對應正式題目的樹林區普通住宅用地案，那兩張表是空白待填的。

**審查模式**吃已填好的書表 PDF，逐格比對並指出填錯的地方，
還會算出那個錯誤讓每平方米的補償價差多少。
用的是官方查估書表範本的金山商業用地案，那是唯一有官方答案可對的案子。

三種介面走同一套計算：CLI、`POST /api/survey/xlsx`、前端畫面。

### 已驗證的數字（個別因素以 0 計）

```
表5-1  總修正數      +23.50%   +14.75%   +14.75%
表4    絕對值加總     29.46%    18.84%    20.24%
       相近程度／權重  較低20%   較高50%   普通30%
       試算價格      170,337   161,577   206,885  元/㎡
       比準地比較價格  176,921 元/㎡
       比準地地價     177,000 元/㎡（查估辦法第 21 條分段進位）
```

局處把表4 的個別因素（項目 7 至 25）填進去之後，絕對值加總、權重排序、
試算價格與比準地地價都會變。活版檔案的 Excel 公式會自動更新下游。

### 回填的格數

```
表3     每張 98 格 × 4 張（23 量測值與勾選 ＋ 56 優劣等級與總級數 ＋ 19 「名稱：無」）
表5-1   355 格（116 級數 ＋ 116 等級文字 ＋ 87 修正百分比 ＋ 24 小計 ＋ 3 總修正數 ＋ 表頭與備註 9）
表4     46 格
輸出    7 個檔案 ＋ 3 份 PDF
```

### 四層驗證

1. **規則集對照官方基準表**（`kernel/tests/test_ruleset_matches_official_pdf.py`）
   29 個細項的修正矩陣與 144 個數值門檻跟《影響地價區域因素評價基準明細表》
   PDF 逐格比對，每個類別型等級的文字都確認真的印在基準表上。
2. **內政部最大影響範圍**（`check_ruleset()` 的 `MOI_CAP_*`）
   查估辦法第 20 條第 2 項要求自訂幅度不得逾越內政部上限。
   28 項零超標，其中 13 項貼齊上限。
3. **計算正確性**（`kernel/tests/`，金山 Golden Case）
   同一套引擎跑官方已填好的金山商業用地案能逐格重現答案。
4. **回填完整性**（`xlsxform/tests/test_audit.py`）
   釘住三張表動過的座標、禁止覆寫範本印好的標籤、
   禁止內部代號與不宜措辭洩漏到交付檔案。

四層都做過突變驗證，故意改壞會紅而不是靜默通過。
另外每次產出附 10 項自我驗證與 `verification-report.json`，
活版的 Excel 公式也用 LibreOffice 的引擎重算過，結果與定版一致且無錯誤值。

測試 365 passed（kernel 181、api 39、parser 44、pdfform 9、xlsxform 92）。
一鍵驗收 `./scripts/verify-all.sh` 七步全通過，約兩分半。

### 這一版的重要修正

依時間順序，都是對照官方手冊發現的實質漏填或錯誤。

- **表4「調整百分率絕對值加總」的定義**。先前誤用區域因素各細項的絕對值加總，
  實際依作業手冊 p.53 是 `|交易日期調整| ＋ |區域因素總修正數| ＋ Σ|個別因素各項|`。
  修正後「權重打平」的問題消失，權重不需人工判斷。
- **表4 表頭六格**整排沒填（估價基準日、案號、比準地宗地流水號、三個實例編號）。
- **表3 的 `Q42`／`Q43`** 是範本印好的欄位名，值欄在 `R42`／`R43`，先前把標籤蓋掉。
- **表3 土地利用現況**整格沒填。
- **表5-1 的 116 格等級文字欄**沒填。優劣等級在書表上是兩欄
  （新北市手冊第 5 章第 42 頁），表頭合併讓它看起來像一欄。
- **表3 的 56 格優劣等級與總級數**沒填。每個細項名稱左邊有兩個窄欄
  （新北市手冊第 3 章第 25 頁的填載範例）。
- **19 格「名稱：無」**沒填。手冊第 3 章第 18 頁允許空白或填「無」，
  範例選擇填「無」，填了才分得出「已勘查確認沒有」與「漏填」。
- **等級文字欄的填法**。三份官方文件三種填法，最後從新北市手冊：
  都市計畫與使用分區填量測值，其餘填優劣文字。
- **交付檔案裡的內部代號**。判級依據印成「50percent 落在…」，
  以及來源寫「口頭指示（工作坊）」那種不適合放進正式書表的措辭。

### 文件

| 文件 | 用途 |
| --- | --- |
| `docs/FILLING-BASIS.md` | 填表依據逐條對照。每一格的手冊條文依據、三處官方文件不一致的取捨 |
| `docs/ARCHITECTURE.md` | 架構決策。確定性與適應性的權衡、四層結構與各層的變動成本 |
| `docs/ROBUSTNESS_AUDIT.md` | 穩健性稽核。壓力測試、風險清單、調整點索引 |
| `.kiro/steering/status.md` | 當前進度與環境指令，每次對話自動載入 |
| `.kiro/steering/decisions.md` | 不要越過的線、官方文件不一致時的取捨原則 |

不進版控的兩類：簡報腳本與預備問答（那不是原始碼，內容會隨場合調整，
存在專案外的私人筆記目錄），以及官方手冊的 OCR 全文（等同官方文件內容，
比照 `*.pdf` 處理，產生方式留在 `scripts/ocr-manual.py`）。

### 這一版沒做的

阻塞部署的兩項，交給部署負責人：

- **API 沒有認證。** 目前任何人都能打 `POST /api/survey/xlsx`。
- **產出檔存在 `tempfile`。** Lambda 之類的無狀態環境會壞，
  要改 S3 或直接回傳位元流。

不阻塞交件的：

- **生成式 AI 為零。** 刻意的，計算不交給模型。要加的位置在 PDF 辨識與
  敘述潤飾，不能取代規則引擎。
- **表3 的 PDF 辨識**（目前只吃 xlsx）。下一步是 Amazon Textract，
  辨識結果必須先攤給人確認。
- **`api/main.py` 的審查模式寫死預設規則集**，這是唯一會安靜給錯答案的地方。
  規則集的 `scope` 與 parser 抽出的 `land_use` 都在，缺把兩者對起來的索引。
- **樹林住宅的個別因素規則集**未編（基準表第 6 到 9 頁是它的來源）。
  題目未提供宗地個別資料，所以編了也算不出東西。
- **表6（宗地市價）**未實作。
