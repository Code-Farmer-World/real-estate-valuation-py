# 可以講的特色與 Demo 腳本

這份給錄影片與現場說明用。收錄的每一條都附「怎麼當場驗證」，
沒有任何一條是話術。講不出驗證方式的就不要講。

紀錄日期 2026-09-12。數字如果變了，跑 `./scripts/verify-all.sh` 取最新的。

---

## 一句話

從地價區段勘查表算出影響地價區域因素明細表與比較法調查估價表，
每一格都指得回官方評價基準明細表的頁碼，而且計算不經過任何模型。

---

## 三個特色

### 一、規則不是人抄的，是跟官方基準表逐格機器對照過的

樹林區普通住宅用地有 29 個區域因素細項，每個細項有一張 5×5 修正率矩陣
與五段等級門檻。這些數字如果抄錯一個，整條計算鏈不會有任何異狀：
判級照樣成功、小計照樣加得起來、自我驗證照樣通過。錯的答案會安靜地
一路走到交件的書表上。

處置方式是反過來驗來源。從官方評價基準明細表 PDF 把矩陣與門檻抽出來，
跟規則集 JSON 逐格比對，兩邊都是機器讀的，不經過人的眼睛。

```bash
python -m pytest kernel/tests/test_ruleset_matches_official_pdf.py -v
```

驗到的東西：28 個矩陣逐格一致（29 個細項扣掉都市計畫那個格式特殊的，
它單獨驗）、每個矩陣是方陣且對角線為 0 且上下三角互為相反數、
18 個數值門檻型細項的 144 個門檻值一致、11 個類別型細項的每個等級
都有寫法對得回基準表、每個細項都記來源頁碼。

144 個門檻只有兩組表示法差異，都寫在測試裡並註明理由：
基準表寫中文「四項以上」而規則集記數字 4；傾斜度的基準表是數值區間
而規則集比對整串文字，因為勘查表填的就是「平均坡度未滿5度」那串字。

**可以強調的**：這組測試做過突變驗證。故意把主要道路寬度的 step 從 3.75
改成 3.0、把建蔽率門檻 80 改成 75、把「大部分規劃及闢建」改成
「大部分已開闢」，三種都會紅並指出是哪一個。測試會失敗才算會守。

### 二、每一格都指得回來源，而且敘述不經過模型

表5-1 的 116 格優劣等級與 87 格修正百分比，每一格都帶著四樣東西：
現地量測值、判到第幾級、判級的依據、在評價基準明細表第幾頁。
矩陣查表的結果也記下來（比準地第幾級對比較標的第幾級查到多少）。

產出的檔案裡附一張「計算依據」工作表，14 個欄位、114 列資料
（87 個細項 × 3 個比較標的的明細，加 24 個群組小計與 3 個總修正數）。
最上面直接寫明每一列都由規則引擎產生，敘述不經過任何模型。

前端畫面上每一列都能點開看判級理由與矩陣查表。

**為什麼不用模型寫敘述**：徵收案會被訴願。同一個案子問兩次要得到一樣的答案，
而且要說得出每個數字的法源。規則式產生的敘述同輸入同輸出。
模型潤飾可以加在這之上,不能取代它。

現場可以打開表5-1 的計算依據工作表，指其中一列念出來，例如主要道路寬度那列：
比準地量到 28 公尺判第 1 級優，比較標的量到 7 公尺判第 5 級劣，
查基準表第 2 頁的矩陣得 +15.00%。

**活版的公式是拿真實試算表引擎驗過的**。`openpyxl` 寫入公式後檔案裡不留
計算結果，所以「Excel 打開會算出什麼」要另外驗。做法是讓 LibreOffice 重算
一次再讀回來，24 格群組小計、3 格總修正數與表4 的 10 個計算欄，
真實引擎算出來的值與定版完全一致，而且沒有任何一格是 `#VALUE!` 這類錯誤。

```bash
python -m pytest xlsxform/tests/test_libreoffice_recalc.py -v
```

這組也做過突變驗證：把 `SUMPRODUCT` 的範圍改成印了單位「M」的欄位，
測試會抓到 `G30 重算後是字串 '#VALUE!'`。

### 三、每次產出都自我驗證，而且交出去的檔案有兩份

十項驗證的做法是把活版 Excel 公式描述的關係，套在定版的數值上重算一次比對。
群組小計是不是等於各細項相加、總修正數是不是等於八個群組小計相加、
表4 的絕對值加總與試算價格是不是符合公式關係、比準地比較價格是不是等於
試算價格的加權平均。任一項不通過，CLI 直接回非零而不會產出檔案。

輸出七個檔案，含一份給收件人看的交付說明。表5-1 與表4 各有活版與定版兩份。活版的加總與四則運算是
Excel 公式，局處把表4 的個別因素填進去之後下游會自動更新。定版全部是
算好的數值，用於對照與轉 PDF。

**為什麼要兩份**：`openpyxl` 寫入公式後檔案裡不會有快取值，用程式讀回來
拿到的是公式字串而不是數字。所以驗證要對定版做，交付可用性要靠活版。

---

## 刻意不做的事（這本身是特色）

計算不交給模型。判級、查表、加總、加權、進位全部在規則引擎裡。
模型算數字沒辦法追溯，徵收案追溯不了就不能用。

不宣稱準確率。這是規則引擎不是預測模型，會被問到就這樣答。
規則對不對可以驗（見特色一），但「準確率 95%」那種說法在這裡沒有意義。

只用官方範本示範。示範資料就是題目給的樹林案與官方空白範本，
沒有自己編的假資料。

表4 的個別因素（項目 7 到 25）留空。題目未提供宗地個別條件資料，
而表4 註記載明那一段由地價查估單位依查估辦法第 20 條辦理。
所以試算價格與比準地地價都是「個別因素以 0 計」的結果,
這個前提寫在表4 的全案備註欄裡。這是資料邊界不是功能缺口。

---

## Demo 腳本

### 開場（30 秒）

講題目：地政局要的是把樹林區四個地價區段的勘查結果，
算成表5-1 的 29 個細項修正率與表4 的比準地地價。
講位置：勘查表是人現地填的，往後兩張表的每一格都是算出來的。

### 一鍵驗收（60 秒，最有說服力）

```bash
cd real-estate-valuation-py && ./scripts/verify-all.sh
```

六步會依序印出：後端 361 passed、規則集與官方基準表逐格一致、
產出七個檔案、回填完整性盤點（每張表動過幾格）、
十項自我驗證逐條列出、前端 type-check 與 16 passed 與 production build。

任何一步失敗就中止並回非零。這一段跑完，正確性的說明就完成大半。

### 走畫面（90 秒）

起後端與前端：

```bash
# 後端
export VALUATION_DOC_DIR="$(cd ../docs/official/real-estate-valuation && pwd)"
export VALUATION_TEMPLATE_DIR="$(cd ../正式題目 && pwd)"
.venv/bin/python -m uvicorn api.main:app --port 8000

# 前端
cd ../real-estate-valuation && npm run dev
```

畫面上的順序：

1. 上傳填好的表3 勘查表 xlsx
2. 最上面先出現「自我驗證通過 10 / 10 項」。先講這個,
   因為它決定下面的數字能不能信
3. 接著是個別因素以 0 計的前提
4. 格數：116 格優劣等級、87 格修正百分比、24 格群組小計、3 格總修正數
5. 表4 的表格：三個比較標的的區域因素調整百分率、調整百分率絕對值加總、
   價格形成因素之相近程度、比較標的權重、試算價格，
   最後是比準地比較價格 176,921 與比準地地價 177,000
6. 切換比較標的，念出規則式產生的那段敘述
7. 點開任一列看判級依據、矩陣查表、基準表頁碼
8. 下載七個檔案

### 交件檔案（可選，20 秒）

要展示 PDF 就跑這一支，三份表各自轉出正確頁數（表3 四頁對應四個地價區段、
表5-1 一頁、表4 一頁）：

```bash
python -m xlsxform.topdf --out ../交付
```

### 收尾（30 秒）

講剩下的邊界，照實講會更可信：生成式 AI 目前為零，位置在 PDF 辨識
與敘述潤飾，不能取代規則引擎。表3 目前吃 xlsx，PDF 辨識下一步接
Amazon Textract，辨識結果必須先攤給人確認再進計算。

---

## 會被問到的問題

**AI 在哪裡？**
目前為零,刻意的。計算交給模型就追溯不了,而徵收案會被訴願。
要加的位置有兩個:把勘查表 PDF 或掃描件辨識成結構化欄位,
以及把規則式敘述潤飾成更好讀的文字。兩者都在規則引擎之外,不取代它。

**準確率多少？**
不宣稱準確率。這是規則引擎不是預測模型。能驗的是規則本身與官方基準表
逐格一致,那有測試,可以現場跑。

**個別因素為什麼是空的？**
題目未提供宗地個別條件資料,表4 註記載明那一段由地價查估單位辦理。
前提寫在表4 全案備註。局處填進去之後,活版的公式會自動把下游更新。

**權重怎麼定的？**
依調整百分率絕對值加總排序,加總最小的最相近給最高權重。
本案 18.84% 最小所以 P003-00 拿 50%,20.24% 拿 30%,29.46% 拿 20%。
絕對值加總的定義是 `|交易日期調整| ＋ |區域因素總修正數| ＋ Σ|個別因素各項|`,
依作業手冊第 53 頁。

**容積率為什麼是 200% 而勘查表寫 260%？**
依地價查估單位說明,本案各區段道路寬度均未達八公尺,一律以 200% 計算。
勘查表忠實記載原載的 260%,計算用 200%,這個區分寫在表5-1 的全案備註。
四個區段同級所以修正率 0.00%。

**能不能換別的行政區？**
規則集是 JSON,換行政區是編一份新的規則集而不是改程式。
目前有兩份:金山商業用地與樹林普通住宅用地。
編法寫在 `.kiro/steering/decisions.md`。

**有沒有認證？**
沒有,已知。部署前要補。現在任何人都能打那支 API。

---

## 數字速查

```
測試        後端 361 passed（kernel 181、api 39、parser 44、pdfform 9、xlsxform 88）
            前端 16 passed，Playwright 端到端 4 passed
規則集      29 個細項、8 個群組、28 個矩陣逐格對照官方基準表、144 個門檻一致
表5-1       116 格優劣等級、87 格修正百分比、24 格群組小計、3 格總修正數
            總修正數 +23.50% / +14.75% / +14.75%
表4         絕對值加總 29.46% / 18.84% / 20.24%
            權重 較低20% / 較高50% / 普通30%
            試算價格 170,337 / 161,577 / 206,885 元/㎡
            比準地比較價格 176,921 元/㎡
            比準地地價 177,000 元/㎡（查估辦法第 21 條分段進位）
回填        表3 每張 23 格 × 4 張、表5-1 239 格、表4 46 格
輸出        7 個檔案（表3、表5-1 活版與定版、表4 活版與定版、驗證報告、交付說明）
自我驗證    10 項
```

三份表的個別因素以 0 計。局處填入後絕對值加總、權重排序、試算價格
與比準地地價都會變。
